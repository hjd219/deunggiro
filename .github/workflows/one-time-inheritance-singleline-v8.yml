name: One-time inheritance mobile single-line v8
on:
  push:
    paths:
      - '.github/workflows/one-time-inheritance-singleline-v8.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/one_time_inheritance_singleline_v8.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add inheritance.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Keep inheritance mobile flow text on one line"
          git push
