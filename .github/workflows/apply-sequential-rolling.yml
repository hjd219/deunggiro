name: Apply sequential rolling (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy one-time patch workflow disabled. Current pages already contain the rolling legal-information behavior."
